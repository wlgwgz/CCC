1、修改底层配置只需修改DAvE文件中的dav文件，并生成代码。
用户的程序必须放在
// USER CODE BEGIN

// USER CODE END
之间，这样重新生成代码就不会删掉用户程序。

2、MCU文件夹包含和MCU相关的所有操作，替换MCU时修改该文件夹。

3、HardWare文件夹包含硬件相关的所有操作，替换硬件时修改该文件夹。

4、以上文件夹的内容一般为只读文件，非特殊需要一般不可随意修改！！
